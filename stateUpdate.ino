void stateUpdate(){
  
  function(&master);
  function(&slave[1]);
  function(&slave[2]);
  function(&slave[3]);
  function(&slave[4]);
  function(&slave[5]);
  function(&slave[6]);
  function(&slave[7]);
  function(&slave[8]);
  function(&slave[9]);

  masterUpdate();

  }
